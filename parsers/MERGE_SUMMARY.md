# YFinance Parser Merge Summary

## Дата об'єднання: 19.10.2025

## Об'єднані файли

1. **parsers/weekly_digest.py** → **parsers/yfinance_parser.py**
2. **parsers/yfinance_parser.py** (старий) → **parsers/yfinance_parser.py** (новий)

## Що було об'єднано

### З weekly_digest.py:

✅ **Ф'ючерсні контракти CBOT** з правильною обробкою цін
- Пшениця (ZW=F)
- Кукурудза (ZC=F)
- Соя (ZS=F)
- Овес (ZO=F)
- Рис (ZR=F)

✅ **Система конверсії**
- Центи → Долари (cents_per_dollar: 100)
- Бушель → Тонна (різні коефіцієнти для кожної культури)
- USD/тонна → UAH/тонна

✅ **Обробка аномалій**
- Спеціальна логіка для рису (ZR=F)
- Виявлення різких стрибків цін (>50%)
- Використання попередніх значень при аномаліях

✅ **Детальний формат повідомлень**
- Пояснення для трейдерів
- Порівняння з українськими цінами
- Формат для щоденних та тижневих звітів

### З yfinance_parser.py (старий):

✅ **ETF та акції компаній**
- WEAT, CORN, SOYB, DBA, CANE, JO (ETF)
- ADM, BG, TSN, MOS (компанії)

✅ **RabbitMQ інтеграція**
- Async публікація повідомлень
- Правильне форматування JSON з UTF-8
- Persistent delivery mode

✅ **Множинні джерела курсу USD→UAH**
- exchangerate-api.com
- NBU (Національний банк України)
- Fallback значення

## Нова структура даних

### COMMODITIES Dictionary

```python
{
    "Commodity Name": {
        "ticker": "SYMBOL",
        "unit": "bushel" | "share" | "cwt",
        "kg_per_unit": float | None,
        "cents_per_dollar": 100 | 1,
        "category": "futures" | "etf" | "company",
        "description": "Ukrainian description",
    }
}
```

### DataFrame Columns

Старі колонки (видалено):
- `Commodity`, `Raw Price`, `USD/share`, `UAH/share`, `USD/ton`, `UAH/ton`

Нові колонки (додано):
- `name`, `ticker`, `category`, `raw_price`, `price_in_dollars`
- `unit`, `usd_per_ton`, `uah_per_ton`, `usd_per_share`, `uah_per_share`
- `description`, `note`

## Нові функції

### 1. `fetch_price_yf(ticker: str)`
- Замінює просте `ticker.history(period="1d")`
- Використовує `period="5d"` для виявлення аномалій
- Спеціальна обробка для ZR=F

### 2. `convert_to_usd_per_ton(price: float, kg_per_unit: float)`
- Точна конверсія з бушеля/cwt у USD/тонна
- Використовує формулу: `price * (1000 / kg_per_unit)`

### 3. `get_commodity_prices(usd_to_uah: float)`
- Єдина функція для обробки всіх типів товарів
- Автоматична конверсія центи→долари
- Розділення на futures/ETF/companies

### 4. `format_telegram_daily_report(df, usd_to_uah)`
- Компактний щоденний звіт
- Розділення по категоріям (🌾 futures, 📈 ETF, 🏭 companies)

### 5. `format_telegram_weekly_digest(df, usd_to_uah, df_ukr)`
- Детальний тижневий дайджест
- Пояснення для трейдерів
- Порівняння з українськими цінами

### 6. `generate_daily_report()` та `generate_weekly_digest()`
- Async функції для генерації звітів
- Автоматична публікація в RabbitMQ
- Збереження weekly digest у `digest.md`

## Видалені функції

❌ `get_price(symbol, name)` - замінено на `fetch_price_yf()`
❌ `make_summary(prices_text)` - видалено HuggingFace summarizer (не використовувався)
❌ `get_usd_to_uah_rate()` - перейменовано на `fetch_usd_to_uah()`
❌ `get_legacy_prices()` - видалено legacy функцію

## Виправлені проблеми

### 1. ❌ Проблема: Ціни були у 100 разів більші
**До:** 503.75 USD/bushel (неправильно)
**Після:** 5.04 USD/bushel (правильно)
**Рішення:** Додано `cents_per_dollar: 100` для futures

### 2. ❌ Проблема: Аномалії в цінах на рис
**До:** Різкий стрибок з 1069 до 10.85
**Після:** Автоматичне виявлення та використання попереднього значення
**Рішення:** Перевірка різниці >50% від середньої

### 3. ❌ Проблема: Неправильна конверсія бушель→тонна
**До:** Використовувався універсальний `factor`
**Після:** Індивідуальний `kg_per_unit` для кожної культури
**Рішення:** Точні коефіцієнти з CBOT специфікацій

### 4. ❌ Проблема: Змішані одиниці вимірювання
**До:** Все намагалося конвертувати в USD/ton
**Після:** Розділення на futures (USD/ton) та shares (USD/share)
**Рішення:** Умовна логіка на основі `unit` та `category`

## Покращення інтеграції RabbitMQ

### Старий формат:
```python
aio_pika.Message(
    body=json.dumps(message_data).encode(),
    delivery_mode=aio_pika.DeliveryMode.PERSISTENT,
)
```

### Новий формат:
```python
aio_pika.Message(
    body=json.dumps(message_data, ensure_ascii=False).encode("utf-8"),
    delivery_mode=aio_pika.DeliveryMode.PERSISTENT,
    content_type="application/json",
    content_encoding="utf-8",
)
```

**Переваги:**
- Правильне кодування українських символів
- Явна вказівка content-type
- UTF-8 encoding для коректного відображення

## Покращення форматування Telegram

### Для ф'ючерсів:
```
• Пшениця (ф'ючерс CBOT): 5.04 USD/bushel ≈ 185.10 USD/т ≈ 7722 ₴/т
```

### Для ETF:
```
• Пшениця (ETF): $4.05 (169 ₴)
```

### Для компаній:
```
• Archer-Daniels-Midland (аграрна компанія): $63.33 (2642 ₴)
```

## Режими роботи

### CLI Interface:
```bash
# Щоденний звіт
python yfinance_parser.py daily

# Тижневий дайджест  
python yfinance_parser.py weekly

# За замовчуванням (daily)
python yfinance_parser.py
```

### RabbitMQ Message Types:
- `commodity_prices_daily` - щоденний звіт
- `commodity_prices_weekly` - тижневий дайджест

## Тестування

### Тест 1: Daily Report
```bash
venv/bin/python yfinance_parser.py daily
```
✅ **Результат:** Успішно опубліковано в RabbitMQ
✅ **Повідомлення:** Містить futures, ETF, companies
✅ **Формат:** Компактний, з емодзі

### Тест 2: Weekly Digest
```bash
venv/bin/python yfinance_parser.py weekly
```
✅ **Результат:** Успішно опубліковано в RabbitMQ + збережено digest.md
✅ **Повідомлення:** Детальне з поясненнями
✅ **Формат:** Розширений, з секціями для трейдерів

## Файли

### Створено:
- ✅ `/parsers/yfinance_parser.py` (новий, об'єднаний)
- ✅ `/parsers/README_YFINANCE_PARSER.md` (документація)
- ✅ `/parsers/MERGE_SUMMARY.md` (цей файл)

### Перейменовано:
- 📝 `/parsers/yfinance_parser.py` → `/parsers/yfinance_parser.old.py` (backup)

### Залишено без змін:
- 📄 `/parsers/weekly_digest.py` (може бути видалений у майбутньому)

## Наступні кроки

### Рекомендації:

1. **Налаштувати cron jobs** для автоматичного запуску:
   ```bash
   # Daily reports at 09:00 and 18:00
   0 9,18 * * * cd /path/to/parsers && venv/bin/python yfinance_parser.py daily
   
   # Weekly digest on Friday at 20:00
   0 20 * * 5 cd /path/to/parsers && venv/bin/python yfinance_parser.py weekly
   ```

2. **Додати monitoring**:
   - Логування в файл
   - Алерти при помилках
   - Метрики успішності публікації

3. **Розширити функціонал**:
   - Додати більше futures (wheat, soybean meal, etc.)
   - Інтеграція з ICE Futures
   - Історичні графіки цін

4. **Покращити Ukrainian prices**:
   - Автоматичний збір з ProZerno, APK-Inform
   - API інтеграція замість CSV
   - Розрахунок базису (difference) автоматично

## Статистика змін

- **Рядків коду (старий):** ~492
- **Рядків коду (новий):** ~677
- **Додано рядків:** ~350
- **Видалено рядків:** ~165
- **Нових функцій:** 6
- **Видалених функцій:** 4
- **Покращених функцій:** 3

## Висновок

✅ Успішно об'єднано функціонал двох парсерів
✅ Виправлено критичну помилку з цінами (100x)
✅ Додано обробку аномалій
✅ Покращено форматування для Telegram
✅ Додано українські переклади
✅ Інтегровано з RabbitMQ
✅ Створено документацію
✅ Протестовано обидва режими

**Статус:** ✅ Готово до production використання

---

**Дата створення:** 19.10.2025
**Автор merge:** GitHub Copilot + Developer Team
**Branch:** yfinance-parcer
