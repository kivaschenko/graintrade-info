### 🚀 Run it

```bash
docker compose up -d
```

Then open:

* **Grafana:** [http://your-server-ip:3000](http://your-server-ip:3000) (login `admin/admin`)
* **Prometheus:** [http://your-server-ip:9090](http://your-server-ip:9090)

---

### 📊 Optional Grafana Dashboards

Import dashboards by ID:

* Node Exporter Full → `1860`
* PostgreSQL Metrics → `9628`
* Redis Overview → `763`
